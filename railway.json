{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "buildCommand": "npm run build" },
  "deploy": { "startCommand": "npm run start && npx prisma migrate deploy" }
}
